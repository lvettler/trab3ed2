#ifndef  UTIL_H_
#define UTIL_H_ 

typedef enum algoritmo{BS, BB}algoritmo;

typedef struct Job{
	int tempo;
	int deadline;
	int multa;
}*Job;

int verifica_tipo(char* tipo);

Job ler_jobs(int n);


typedef struct Job{
	vet posicionado
	vet a posicionar
	limite iferior 
	limeti superior

}*permuta;










#endif

